import { seedCourses } from "./courses";
import { seedChapters } from "./chapters";
import { seedSections } from "./sections";
import { seedExercises } from "./exercises";
import { seedUsers } from "./users";
import { seedFeedback } from "./feedback";
import { seedNotes } from "./notes";
import { seedStudentExerciseProgress } from "./student-exercise-progress";
import { seedStudentProgress } from "./student-progress";

export const seeders = {
    seedCourses,
    seedChapters,
    seedSections,
    seedExercises,
    seedUsers,
    seedFeedback,
    seedNotes,
    seedStudentExerciseProgress,
    seedStudentProgress
}
